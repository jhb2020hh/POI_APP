{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    // Die Vercel-Function importiert die gebaute App aus dist/. Ohne
    // Typdeklarationen waere dieser Import fuer TypeScript ein any.
    "declaration": true
  },
  "include": ["src"]
}
